# Template only — do NOT commit a real token.
# Prefer creating the Secret imperatively (see README), or copy this to
# pulumi-token-secret.yaml (gitignored), fill in the token, and apply it.
apiVersion: v1
kind: Secret
metadata:
  name: pulumi-api-secret
  namespace: demo
type: Opaque
stringData:
  accessToken: "pul-REPLACE_WITH_YOUR_TOKEN"
